/*import { describe } from 'riteway';

import { reducer, click } from '../ExternalLinkPanel/ExternalLinkPanelReducer';

describe('click counter reducer', async assert => {
  assert({
    given: 'no arguments',
    should: 'return the valid initial state',
    actual: reducer(),
    expected: 0
  });
});*/